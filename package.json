{
  "name": "wiredwave-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "start": "cd dist && python3 -m http.server 8000 --bind 127.0.0.1",
    "verify": "diff -u index.html dist/index.html || true"
  }
}
